# 主题配置

配置文件位于 `src/theme/settings.ts`。

## 主要配置项

- `themeScheme`：`'light'` | `'dark'` | `'auto'`
- `themeColor`：主题色
- `layout.mode`：布局模式
- `tab.visible`：标签页可见性
- `sider.width`：侧边栏宽度

## 存储

- 开发模式：实时生效
- 生产环境：缓存到 localStorage
